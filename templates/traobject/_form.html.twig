{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_row(form.title) }}
    {{ form_row(form.eventAt) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
